<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.gatech.seclass.sdpcryptogram">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LandingPageActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CreateAccountActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainMenuActivity" />
        <activity android:name=".ViewMenuActivity" />
        <activity android:name=".CreateCryptogramActivity" />
        <activity android:name=".ChooseCryptogramActivity" />
        <activity android:name=".SolveCryptogramActivity" />
        <activity android:name=".ViewCompleteCryptogramsActivity" />
        <activity android:name=".ViewIncompleteCryptogramsActivity" />
        <activity android:name=".ViewStatisticsActivity"></activity>
    </application>

</manifest>